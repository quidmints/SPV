[package]
name = "quid-api"
version = "0.1.11"
license = "MIT"
description = "Quid API types, definitions, client/server utils, and TLS config"
documentation = "https://docs.rs/quid-api"
authors = { workspace = true }
edition = { workspace = true }
readme = { workspace = true }
repository = { workspace = true }
rust-version = { workspace = true }
publish = true

# Default config for testing in SGX
[package.metadata.fortanix-sgx]

[features]
default = []
# Enables various test hooks and utilities. Should be disabled in staging/prod.
test-utils = [
  "dep:proptest",
  "dep:proptest-derive",
  "quid-api-core/test-utils",
  "quid-common/test-utils",
  "quid-tls/test-utils",
]

[dependencies]


quid-api-core = { workspace = true, features = ["axum", "reqwest"] }
quid-common = { workspace = true }
quid-crypto = { workspace = true }
quid-std = { workspace = true }
quid-tls = { workspace = true }
quid-tokio = { workspace = true }

# --- WORKSPACE --- #

anyhow = { workspace = true }
axum = { workspace = true, features = ["json", "query"] }
axum-server = { workspace = true }
bcs = { workspace = true }
bytes = { workspace = true }
http = { workspace = true }
http-body-util = { workspace = true }
hyper = { workspace = true, features = ["http1", "http2", "server"] }
lightning = { workspace = true }
rand_core = { workspace = true }
reqwest = { workspace = true, features = ["json"] }
rustls = { workspace = true }
semver = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true, features = [] }
tower = { workspace = true, features = ["buffer", "limit", "load-shed", "timeout", "util"] }
tower-http = { workspace = true, features = ["trace"] }
tracing = { workspace = true }

# --- OPTIONAL --- #
# These must have `optional = true` to prevent infecting production binaries.

proptest = { optional = true, workspace = true, features = ["alloc"] }
proptest-derive = { optional = true, workspace = true }

[dev-dependencies]
quid-common = { workspace = true, features = ["test-utils"] }

axum = { workspace = true }
proptest = { workspace = true, features = ["alloc"] }
proptest-derive = { workspace = true }
tokio = { workspace = true, features = [
    "test-util",
    "time"
] }
